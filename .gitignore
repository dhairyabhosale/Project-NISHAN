# dependencies
node_modules/

# next.js build output
.next/
out/
next-env.d.ts

# typescript incremental build info
*.tsbuildinfo

# environment - never commit real keys (CLAUDE.md §12.6)
.env
.env.*
!.env.example

# local scratch / caches
work/
outputs/

# vendored skill packages - tooling, not project source
.claude/skills/
.agents/

# os / editor
Thumbs.db
.DS_Store
*.swp

# test build output
.test-build/
*.tmp.js
.env*

# The ungraded hero source. The hero is a WebGL gradient now and nothing is
# derived from this file any more, so it is dead weight - but it is 3.35 MB of
# someone else's download sitting untracked in the working tree, and the rule
# stays so an absent-minded "git add ." cannot commit it. Safe to delete.
growxai-*.mp4
.vercel
