# dependencies
node_modules/

# next.js build output
.next/
out/
next-env.d.ts

# typescript incremental build info
*.tsbuildinfo

# environment - never commit real keys (CLAUDE.md §12.6)
.env
.env.*
!.env.example

# local scratch / caches
work/
outputs/

# vendored skill packages - tooling, not project source
.claude/skills/
.agents/

# os / editor
Thumbs.db
.DS_Store
*.swp

# test build output
.test-build/
*.tmp.js
.env*

# The ungraded hero source. 3.35 MB, does not loop, and everything shipped is
# derived from it by the ffmpeg chain recorded in BUILDLOG. Keeping it out of
# the repo keeps a clone small; keeping the recipe in the log keeps it
# reproducible.
growxai-*.mp4
